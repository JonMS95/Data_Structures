{
    "tasks": [
        {
            "type": "shell",
            "label": "Compile",
            "command": "g++ -g ${config:pathToSourceFiles}/* -o ${config:pathToExecutableFiles}/main",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "C/C++: gcc build main.c and add.c files. Generate test executable file and test it."
        }
    ],
    "version": "2.0.0"
}